import colors from "../colors";
import { Theme } from "../theme";

const theme: Theme = {
  color: {
    ...colors,
    brand01: "#006162",
    brand02: "#009e9b",
    brand03: "#20d5d2"
  }
};

export default theme;
