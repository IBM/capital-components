import colors from "../colors";
import { Theme } from "../theme";

const theme: Theme = {
  color: {
    ...colors,
    brand01: "#6f28cc",
    brand02: "#924cfc",
    brand03: "#bc8aff"
  }
};

export default theme;
